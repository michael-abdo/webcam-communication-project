#\!/bin/bash
# Deploy to Heroku using GitHub source

echo "The latest video loading fix has been committed to GitHub:"
echo "Commit: 0c8b935 - Fix video loading by restoring JSON response handling"
echo ""
echo "To deploy to Heroku, you can:"
echo "1. Go to Heroku Dashboard > Deploy tab"
echo "2. Connect to GitHub if not already connected"
echo "3. Deploy the 'master' branch manually"
echo ""
echo "Or use Heroku CLI after authentication:"
echo "heroku login"
echo "git push heroku master"
